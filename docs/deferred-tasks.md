# Tâches reportées / laissées de côté

> **Mise à jour automatique** : ce fichier est régénéré chaque lundi à 06h00 UTC par l'edge function `update-deferred-tasks` (cron `cron-update-deferred-tasks`).
> Pour forcer une régénération : `supabase.functions.invoke('update-deferred-tasks')`.
>
> Sources scannées :
> - `knowledge/**/*.md` & `.lovable/memory/**/*.md` — marqueurs `⏳`, `TODO`, `FIXME`, `non porté`, `non portée`, `hors scope`, `sprint dédié`
> - Fichiers code `src/**` & `supabase/functions/**` — commentaires `// TODO`, `// FIXME`, `// XXX`, `// HACK`
> - Table `architect_workbench` (status `pending` depuis > 30 jours)
>
> Dernière génération : _en attente du premier run_

---

## Sommaire

- [Backlog produit (knowledge/memory)](#backlog-produit)
- [Dette technique (code)](#dette-technique)
- [Tâches d'audit > 30j (architect_workbench)](#tâches-daudit-non-traitées)

---

## Backlog produit

_Aucune entrée — exécutez l'edge function `update-deferred-tasks` pour peupler._

## Dette technique

_Aucune entrée — exécutez l'edge function `update-deferred-tasks` pour peupler._

## Tâches d'audit non traitées

_Aucune entrée — exécutez l'edge function `update-deferred-tasks` pour peupler._
