# Audit Crawl Multi-Pages — 2026-07-08
Date : 2026-07-08 · Auditeur : Lovable · Brief : `brief-2026-07-08.md`

## Périmètre audité
`crawl-site` (690 l.), `process-crawl-queue` (309 l.), `cron-crawl-scheduler` (264 l.), `strategic-crawl` (375 l.), `_shared/crawlQueue/` (1 155 l.), `SiteCrawl.tsx` (1 327 l.), `useCrawlEngine.ts` (897 l.), `useSiteCrawl.ts` (735 l.), tables `site_crawls`, `crawl_pages`, `crawl_jobs`, `site_crawl_schedule`.

## Résumé exécutif
Cascade 4 sources solide (renderPage → Spider → Firecrawl → Browserless), checkpoint recovery, BFS depth, incrémental 12h OK. **4 bloquants** : crédit figé à 1, Premium plafonné comme Free, `crawl_pages` sans UPDATE policy, zéro télémétrie.

## Findings

### C1 · P0 · 0.5j — Credit cost figé à 1 crédit vs 5-40 brief
`crawl-site/index.ts:431-433` → `creditCost = 1` constant. Un crawl 500p sous-facturé 40×.
**Fix** : shared `_shared/crawlCreditCost.ts` avec grille 5/10/15/25/40.
| Perf | Sécu | Coût | Maint | **Global** | 
|---|---|---|---|---|
| 8 | 8 | 1 | 6 | **5.75** |

### C2 · P0 · 0.25j — Plan Premium solo = Free (50p max)
`crawl-site/index.ts:382,405` : seul `agency_pro/premium` escape la limite 50p. Premium/Pro solo restent bloqués.
**Fix** : branches `premium/pro → 150p`.

### C3 · P0 · 0.25j — `crawl_pages` sans policy UPDATE
Migrations `20260311124348/400` : SELECT+INSERT only. Bypass silencieux service_role.
**Fix** : `CREATE POLICY` UPDATE service_role + DELETE authenticated via join `site_crawls`.

### C4 · P1 · 0.5j — Zéro télémétrie parcours crawl
0 `trackEvent` dans `SiteCrawl.tsx`, `useCrawlEngine`, `useSiteCrawl`. Roadmap à l'aveugle.
**Fix** : 5 events (`crawl_launched`, `_detect_completed`, `_analyze_started`, `_export_pdf`, `_compare_clicked`).

### C5 · P1 · 0.5j — 31 `: any` dans `finalizer.ts`
`finalizeJob(supabase: any, job: any)`, `feedKeywordUniverse`, `generateAISummary` non typés.
**Fix** : `_shared/crawlQueue/dbTypes.ts` avec `CrawlJob`/`CrawlPage`.

### C6 · P1 · 1j — Hooks 897+735 LOC (useCrawlEngine/useSiteCrawl)
Polling + compare + detect + stats + realtime dans un seul hook.
**Fix** : découpe `useCrawlPolling` / `useCrawlCompare` / `useCrawlDetect` / `useCrawlStats` (< 250 l.).

### C7 · P1 · 0.5j — `strategic-crawl` : 375 l. de code mort
0 appel front (`rg strategic-crawl src/` vide). Ni lecture ni écriture `crawl_pages`.
**Fix** : intégrer signaux E-E-A-T dans `htmlAnalyzer.ts` shared, archiver la fonction standalone.

### C8 · P1 · 0.25j — Gemini `gemini-3-flash-preview` sans fallback
`finalizer.ts:307` : modèle preview instable, pas de retry.
**Fix** : `google/gemini-flash-1.5` + fallback `google/gemini-pro` dans catch.

### C9 · P1 · 0.25j — Voice-tone trigger ANON_KEY → 401 silencieux
`finalizer.ts:403` : `Bearer ANON_KEY`, erreur swallowed.
**Fix** : `SERVICE_ROLE_KEY` (comme `process-crawl-queue:283`).

### C10 · P1 · 0.5j — IKtracker hardcodé dans fonction publique
`crawl-site/index.ts:292-342` : routes `/marina`, `/offline` embarquées.
**Fix** : `_shared/tenantOverrides/iktracker.ts` ou `tracked_sites.custom_config`.

### C11 · P2 · 0.5j — Thundering herd self-relay
`process-crawl-queue:271-292` : worker + cron + `crawl-site` peuvent lancer N instances sur même job.
**Fix** : `pg_try_advisory_xact_lock(job.id)` ou cooldown 30s.

### C12 · P2 · 0.5j — `discoverDirectories` query non-aggregée
`cron-crawl-scheduler:38-42` : 2000 rows × 150 domaines = 300k rows/run.
**Fix** : RPC `get_crawl_directories` avec `GROUP BY split_part(path,'/',2)`.

### C13 · P2 · 0.25j — `site_crawl_schedule` accessible service_role only
Pro Agency ne peut pas consulter/modifier son planning.
**Fix** : policies SELECT+UPDATE `auth.uid() = user_id`.

## Cumul dette feature
**Total : 5.75j** — P0 : 1j (revenus/RLS) · P1 : 3.5j · P2 : 1.25j.

### Points forts
- Cascade 4 sources + SPA probe conforme brief
- Checkpoint recovery `crawl_pages` count reconciliation solide
- BFS depth + duplicate detection + intent classification complets
- Modularisation `_shared/crawlQueue/` (5 modules) respectée
- RLS `site_crawls` / `crawl_jobs` owner-scoped cohérentes
