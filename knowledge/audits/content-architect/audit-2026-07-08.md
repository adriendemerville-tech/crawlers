# Audit — Content Architect
Date : 2026-07-08 · Brief : `brief-2026-07-08.md` · Baseline : première itération

## Périmètre confirmé
- `content-architecture-advisor/index.ts` — **1 642 l., 43 `console.*`, 49 `: any`**, utilise `aiGatewayFetch` ✅
- `_shared/contentArchitectCache.ts` (buildCacheKey, getUserCache, getCrawlersRecommendation, saveCache, payloadToMarkdown) ✅
- `extract-architect-fields/`
- Front : `CocoonContentArchitectModal`, `ContentArchitectPage.tsx` (980 l. backlog)

## Findings
| # | Sévérité | Problème |
|---|---|---|
| CA1 | **P0** | Monolithe 1 642 l. avec pipeline 4 étages (briefing/stratège/rédacteur/tonalisateur) dans un seul handler — impossible à tester unitairement |
| CA2 | **P0** | **49 `: any`** — pipeline retourne des `any` à chaque étage → aucune garantie de forme du draft final avant push CMS |
| CA3 | P1 | 43 `console.log` — remplacer par logger structuré (correlation_id par draft) |
| CA4 | P1 | Skill `generate_content_architecture` non déclaré dans Copilot → Stratège ne peut pas générer un plan éditorial en autonomie |
| CA5 | P2 | Cache mutualisé OK (RPC SECURITY DEFINER `get_shared_architect_recommendation`) — vérifier `hit_count` réel en prod (SQL fournie brief) |
| CA6 | OK | Voice DNA injectée conformément à `voice-dna-fr` |

**Découpe cible** : `_shared/contentArchitect/{briefing,strategist,writer,tonalizer,types}.ts` + handler `index.ts` ≤ 300 l.

Score global feature : **6/10** (fonctionne, mais dette bloquante pour scale).
