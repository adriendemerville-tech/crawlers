# Audit — Marina (prospection B2B)
Date : 2026-07-08 · Brief : `brief-2026-07-08.md` · Baseline : première itération complète

## Périmètre confirmé
- `supabase/functions/marina/index.ts` — **2 984 l., 87 `console.*`, 57 `: any`** ❌ pire du projet
- `view-marina-report/`
- Front : `src/pages/Marina.tsx` (1 510 l. backlog)
- Utilise `LOVABLE_API_KEY` en direct (fetch nu) — **PAS de wrapper `aiGatewayFetch`**

## Findings
| # | Sévérité | Problème |
|---|---|---|
| M1 | **P0** | **Monolithe 2 984 l.** — le plus gros edge du projet, tout dans un seul handler (prospect scoring, SEO snapshot, GEO snapshot, spiral, LLM outreach) |
| M2 | **P0** | **57 `: any`** — payloads prospects, résultats SEO/GEO/Spiral tous non typés → risque de régression majeur dès qu'une brique amont change de shape |
| M3 | **P0** | Utilisation `LOVABLE_API_KEY` en `fetch` direct au lieu de `aiGatewayFetch` shared → pas de kill switch, pas d'observabilité `ai_gateway_usage` fiable, pas de fallback |
| M4 | **P0** | Duplication probable des modules SEO/GEO/Spiral au lieu de les invoquer → contradiction avec brief §6 "cross-check non-duplication". À confirmer via `grep computeSeoScoreV2\|spiralClassifier` dans marina/ |
| M5 | P1 | 87 `console.log` sans logger structuré — impossible de tracer un run outreach précis |
| M6 | P1 | Skill Copilot Marina absent — Stratège ne peut pas déclencher un audit prospect |

**Découpe cible** : `_shared/marina/{prospectScoring,seoSnapshot,geoSnapshot,spiralSnapshot,outreachLLM,whiteLabel,types}.ts` — chaque module réutilise les fonctions partagées existantes (`computeSeoScoreV2`, `spiralClassifier`, `geo-kpis-aggregate` via invoke) et non des copies locales.

Score global feature : **3/10** (fonctionne mais dette + duplication + hors gateway = triple risque).
