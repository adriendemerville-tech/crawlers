# Audit Brief — Marina (Vague 4-bis, périmètre élargi)
Date : 2026-07-08 · Auditeur : Lovable · Baseline : `knowledge/audits/marina/audit-2026-07-08.md` (Vague 4, monolithe backend uniquement)

## 1. Objectif métier
Compléter l'audit Vague 4 Marina en couvrant les composants NON audités du workflow (front, admin, tables, pipeline outreach, modules `_shared`) et confirmer par preuves les hypothèses laissées ouvertes (notamment M4 duplication).

## 2. Cas d'usage principaux
- Pro Agency → `src/pages/Marina.tsx` → lance un audit prospect + consulte rapport white label
- Admin → `MarinaDashboard.tsx` / `ProspectPipelineDashboard.tsx` → supervise runs, quotas, qualité
- Prospect → reçoit outreach LinkedIn depuis `outreach_sequences` + `prospect_outreach_queue`
- Copilot Stratège → skill `marina_prospect_audit` (absent — cf. M6) → devrait invoquer Marina et indexer en vector memory

## 3. Logique métier clé
- Confirmation Vague 4 (grep exécuté ce jour) : `marina/index.ts` **ne référence AUCUN** `computeSeoScoreV2`, `spiralClassifier`, `geo-kpis-aggregate`, ni `functions.invoke` → **M4 est un fait, plus une hypothèse : duplication complète des logiques SEO/GEO/Spiral à l'intérieur du monolithe**
- Confirmation M3 : `LOVABLE_API_KEY` en `Deno.env.get` direct (ligne 1519), pas de `aiGatewayFetch`
- Tables du workflow : `marina_prospects` (21 col., 1 policy), `marina_training_data` (34 col., 1 policy), `marina_api_keys` (9 col., 2 policies), `prospect_outreach_queue` (16 col., 1 policy), `outreach_sequences` (9 col., 4 policies), `outreach_daily_quotas` (9 col., 3 policies), `outreach_events` (8 col., 2 policies)
- Front lourd : `src/pages/Marina.tsx` (1 510 l., backlog front)
- Admin : `MarinaDashboard.tsx`, `MarinaReportPreviewModal.tsx`, `ProspectPipelineDashboard.tsx`
- Contraintes mémoire : `features/prospecting/marina-module-fr`, `features/prospecting/outreach-pipeline-fr`, `constraints/social-auth-limitations-fr`, `features/copilot/vector-memory-hybrid-recall`, `features/copilot/prompt-safety-layer`

## 4. Critères de qualité attendus
| Axe | Cible | Mesure |
|---|---|---|
| RLS/GRANT | 7 tables Marina/outreach conformes (auth.uid, GRANT service_role) | `security--run_security_scan` + `read_query` policies |
| Front modularité | `Marina.tsx` < 400 l. par sous-composant, 0 `: any`, hooks isolés | revue + `rg ": any" src/pages/Marina.tsx` |
| Admin cohérence | `MarinaDashboard` réutilise les mêmes selects que le front public (pas de duplication de requêtes) | grep |
| Pipeline outreach | quotas respectés, backoff LinkedIn, retry idempotent, RLS par owner | revue edge + tables |
| Copilot | skill `marina_prospect_audit` déclaré dans `skills/registry.ts`, résultat wrappé `wrapToolResult('marina')` pour vector memory | grep registry |
| Prompt safety | payloads prospect (nom société, description) passés dans `<user_input>` avant LLM | grep `<user_input>` dans marina |
| UX | palette violet/or/noir/blanc, boutons bordure sans fond, pas d'emoji, pas de bleu IA | revue Marina.tsx + composants |
| Duplication (M4 bis) | plan de découpe `_shared/marina/*` référençant `computeSeoScoreV2` + `spiralClassifier` + invoke `geo-kpis-aggregate` | livrable |

## 5. Benchmark concurrentiel
Mêmes concurrents que le brief initial (Apollo, Lemlist, Waalaxy, Clay, Similarweb Sales Intelligence). Nouveauté ciblée par ce brief-bis : **la surface UI/UX admin+prospect** — comparer à Lemlist (dashboard campagne clair) et Apollo (fiches prospect denses) pour scorer `Marina.tsx` et `ProspectPipelineDashboard.tsx`.

## 6. Périmètre d'audit
### In scope (Vague 4-bis)
- Frontend : `src/pages/Marina.tsx`, `src/components/Admin/{MarinaDashboard,MarinaReportPreviewModal,ProspectPipelineDashboard}.tsx`
- Backend annexe : `supabase/functions/view-marina-report/index.ts` (scoring détaillé)
- Modules `_shared` consommés : `marinaWorkbench.ts`, `topPriorities.ts` (angle Marina), `identityGateway.ts` (angle Marina)
- Pipeline outreach : recherche des edges `outreach-*` / `prospect-*` + tables associées
- Tables : `marina_prospects`, `marina_training_data`, `marina_api_keys`, `prospect_outreach_queue`, `outreach_sequences`, `outreach_daily_quotas`, `outreach_events`
- Confirmation formelle M4 (duplication) et plan de découpe `_shared/marina/*`
### Hors scope
- Réaudit de `marina/index.ts` monolithe (déjà couvert Vague 4 → refactor à planifier séparément)
- Doc dev `MarinaApiDoc.tsx` (page statique)

## 7. Signaux à mesurer
- SQL RLS : `select tablename, policyname, cmd, qual from pg_policies where tablename in ('marina_prospects','marina_training_data','marina_api_keys','prospect_outreach_queue','outreach_sequences','outreach_daily_quotas','outreach_events')`
- SQL usage : `select edge_function, count(*), avg(total_cost) from ai_gateway_usage where edge_function like 'marina%' or edge_function like 'outreach%' group by 1`
- Grep duplication : déjà exécuté — 0 réutilisation confirmée
- Grep front : `rg -n ": any|console\\." src/pages/Marina.tsx | wc -l`
- Registry Copilot : `rg -n "marina" supabase/functions/_shared/skills/registry.ts`
- Analytics : `marina_audit_launched`, `marina_prospect_contacted`, `marina_export_pdf`, `outreach_step_sent`
